const postBaseURL = `https://jsonplaceholder.typicode.com`
const spaceBaseURL = `https://api.spacexdata.com/v3`

const posts = `/posts`
const launches = `/launches`

const urls = {
    posts,
    launches
}


export {
    postBaseURL,
    spaceBaseURL,
    urls
}